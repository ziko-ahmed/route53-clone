fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
pydantic==2.10.4
python-multipart==0.0.20

# Only needed when DATABASE_URL points at PostgreSQL.
# Local development uses SQLite, which is built into Python.
psycopg[binary]==3.2.9
